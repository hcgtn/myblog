doctype html
html
  head
    meta(charset='utf-8')
    meta(name='renderer', content='webkit')
    meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    meta(name='viewport', content='width=device-width, initial-scale=1, maximum-scale=1')
    title 404
    style.
      html,body{height:100%}
      body{margin:0;}
      #img{
      overflow:hidden;
      width:100%;
      height:100%;
      background:url('./imgs/404.jpg') no-repeat center center;
      background-size: 100% auto;
      }
      #seconds{
      color:red;
      }
      h2{
      text-align:center;
      margin-top:100px;
      }
  body
    #img
      h2
        span#seconds 3
        |  秒后跳转到首页。。
    script.
      let n = 3;
      let time = null;
      const fn = ()=>{
      seconds.innerHTML = --n;
      if(n===0){
      location.href = "/";
      clearInterval(time);
      };
      };
      time = setInterval(fn,1000);
